import React from 'react';

const Project = (props) => {
    return (
        <article className="project">
            <h1>
                {props.id}
            </h1>
        </article>
    );
}

export default Project;